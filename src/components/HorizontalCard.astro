---
const { title, img, imgDark, desc, url, badge, tags, target = "_blank", format="webp", custom_tags_url } = Astro.props;
import { Image } from "astro:assets";
const tag_url = custom_tags_url 
  ? custom_tags_url 
  : url.split("/").slice(0, -1).join("/") + "/tag";
const badges = Array.isArray(badge) ? badge.flat() : [badge];
---

<div
  class="rounded-lg bg-base-100 hover:shadow-xl transition ease-in-out hover:scale-[102%]"
>
  <a href={url} target={target}>
    <div class="flex flex-row item-start p-3 gap-4">
      {
        img && (
          <Image
            src={img}
            width={750}
            height={422}
            format={format}
            alt={title}
            class={`w-[11rem] md:w-[10rem] rounded-lg ${imgDark ? "block dark:hidden" : ""}`}
          />
        )
      }
      {imgDark && (
        <Image
          src={imgDark}
          width={750}
          height={422}
          format={format}
          alt={title}
          class="w-[11rem] md:w-[10rem] rounded-lg hidden dark:block"
        />
      )}
      <div class="grow w-full">
        <h1 class="text-lg font-bold">
          {title}
          {
            badges && 
              badges.filter(Boolean).map((b) => (
                <div class="badge badge-secondary badge-sm mx-1">
                  {b}
                </div>
              ))
          }
        </h1>
        <p class="py-1 text-sm">{desc}</p>
        <div class="card-actions justify-begin">
          {
            tags &&
              tags.map((tag) => (
                <a href={`${tag_url}/${tag}`} class="badge badge-outline badge-sm">
                  {tag}
                </a>
              ))
          }
        </div>
      </div>
    </div>
  </a>
</div>